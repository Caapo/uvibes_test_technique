// uvibes-homepage/next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['randomuser.me'],
  },
};



export default nextConfig;

module.exports = nextConfig;

